#![allow(incomplete_features, trivial_bounds)]
#![feature(layout_for_ptr, trait_upcasting, trivial_bounds, downcast_unchecked)]

pub mod instance;

// pub fn test() {
//     // use instance::prelude::*;

//     // let lua = Lua::new();
//     // let mut globals = lua.globals();
//     // globals.set("myInstance", Instance::new::<Part>(None));
// }
