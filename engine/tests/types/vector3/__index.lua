local vector = Vector3.new()
assert(vector.X, 0)
assert(vector.Y, 0)
assert(vector.Z, 0)
assert(vector.Magnitude, 0)

-- lowercase indexes
assert(vector.x, 0)
assert(vector.y, 0)
assert(vector.z, 0)
assert(vector.magnitude, 0)

